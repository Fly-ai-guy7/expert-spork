# Current Priority

**Complete the discovery picture on the Mac before any build decision is
final.**

This session audited everything reachable (the `expert-spork` repository:
Luxor Guest House + RxEgypt Pilot) to full depth, but 23 named projects —
including both proposed pilot donors, Atlas Voyage and Marina Ember — exist
only on the Mac and were not observable remotely.

The single highest-leverage move is therefore: run this same audit brief in a
Claude Code session **on the Mac**, extending `project-registry.json` and
`port-registry.json` in place. Every downstream decision (travel-template
donor, pilot order, port map, archive candidates) is parked behind that.

Secondary priority, unblocked and safe to approve independently: the
factory skeleton + `packages/design-tokens` (steps 2–3 of
`MIGRATION_SEQUENCE.md`), because they depend only on evidence already
confirmed in this repo.
