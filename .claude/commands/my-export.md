---
description: Exports the conversation as plain text.
argument-hint: "[filename]"
---

Export the current conversation as plain text. Use $ARGUMENTS as the output filename if provided, otherwise use a default name with timestamp.
