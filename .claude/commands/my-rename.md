---
description: Renames the current session. Without a name, auto-generates one.
argument-hint: "[name]"
---

Rename the current session to $ARGUMENTS. If no name is provided, auto-generate one based on the session content.
