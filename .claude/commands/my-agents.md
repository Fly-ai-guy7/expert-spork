---
description: Manages agent (sub-agent) configurations.
---

Manage agent (sub-agent) configurations. Support listing, creating, editing, and deleting agents stored in `.claude/agents/` (project) or `~/.claude/agents/` (user).
