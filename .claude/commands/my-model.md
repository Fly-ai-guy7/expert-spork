---
description: Switches between models (Sonnet, Opus, Haiku). Takes effect immediately.
argument-hint: "[model]"
---

Switch the active model to $ARGUMENTS (e.g., sonnet, opus, haiku). The change takes effect immediately for subsequent turns. If no argument is provided, list the available models.
