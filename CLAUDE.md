# CLAUDE.md

## Project Overview

**expert-spork** is a newly initialized repository. It currently contains no application code, build system, or dependencies. This file serves as the foundation for AI assistant guidance as the project develops.

## Repository Structure

```
expert-spork/
├── CLAUDE.md          # AI assistant guidance (this file)
├── README.md          # Project README
└── .git/              # Git metadata
```

## Development Setup

No build system, package manager, or dependencies are configured yet. When they are added, update this section with:

- Language/framework and version requirements
- Install commands (e.g., `npm install`, `pip install -r requirements.txt`)
- Environment variable setup
- Database or service dependencies

## Common Commands

None configured yet. Update this section as tooling is added:

- **Build**: _TBD_
- **Test**: _TBD_
- **Lint**: _TBD_
- **Format**: _TBD_

## Git Workflow

- **Main branch**: `main`
- **Commit style**: Use clear, descriptive commit messages
- **Branching**: Feature branches off `main`

## Conventions for AI Assistants

- Read existing code before proposing changes
- Do not over-engineer; keep changes minimal and focused
- Do not add files, dependencies, or abstractions unless explicitly requested
- When the project gains a test suite, run tests before considering work complete
- When the project gains a linter, ensure code passes lint checks
- Prefer editing existing files over creating new ones
